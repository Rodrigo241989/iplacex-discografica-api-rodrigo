# ============================================================
# Configuración de la aplicación Spring Boot - Discográfica
# ============================================================

# Nombre de la aplicación
spring.application.name=discografia2

# ── Conexión a MongoDB Atlas ─────────────────────────────────
# La URI se obtiene de la variable de entorno MONGODB_URI
# Si no existe la variable, usa el valor por defecto (desarrollo)
# En Render: configurar MONGODB_URI en Environment Variables
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://eva2_spring:GIhMo3ua0acgTztw@cluster-spring.znbiq9h.mongodb.net/discografica-db?appName=cluster-spring}

# Nombre de la base de datos en MongoDB
spring.data.mongodb.database=discografica-db

# ── Puerto del servidor ──────────────────────────────────────
# Render asigna el puerto mediante la variable PORT
# En local usa 8080 por defecto
server.port=${PORT:8080}
